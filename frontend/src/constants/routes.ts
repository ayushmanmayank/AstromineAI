// Central route path constants for the AstroMineAI frontend.
export const ROUTES = {
  home: "/",
  dashboard: "/dashboard",
  upload: "/upload",
  prediction: "/prediction/demo-001",
  predictionById: "/prediction/:id",
  history: "/history",
  about: "/about",
} as const;
