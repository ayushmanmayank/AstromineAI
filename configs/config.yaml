# AstroMineAI Month 1 configuration
#
# Slice 1 scope: verified data acquisition only (Vesta, Dawn FC + Dawn VIR).
# All URLs below were resolved and spot-checked by hand against the live
# PDS Small Bodies Node archive on 2026-08-26/27 (see docs/month1_log.md).

target_body: vesta

data:
  raw_dir: datasets/raw
  processed_dir: datasets/processed
  metadata_dir: datasets/metadata

  # Slice 2 (spatial_alignment.py) parameters.
  # min_spatial_correspondence_confidence: 0.3 is a starting threshold, not
  # a validated cutoff — pick it low enough to not silently discard every
  # marginal pair while we still have so few candidates to look at. Revisit
  # once the Month 1 data audit has real confidence-score distributions to
  # look at.
  min_spatial_correspondence_confidence: 0.3
  # Time window used to shortlist FC/VIR candidate pairs before doing any
  # geometry math. 24h covers same-orbit-pass and same-observation-sequence
  # timescales without being so wide it's meaningless.
  max_time_delta_hours: 24.0

  # Slice 3 (splits.py) parameters. Region-based split — see
  # src/utils/splits.py's assign_region_based_splits().
  splits:
    train: 0.7
    val: 0.15
    test: 0.15
    seed: 42
    bin_size_degrees: 5.0

  sources:
    # ------------------------------------------------------------------
    # Dawn Framing Camera (FC2), Vesta mission phase.
    #
    # Confirmed live node: PDS Small Bodies Node (SBN), hosted at PSI.
    #   - pds-smallbodies.astro.umd.edu is the node's legacy domain; it
    #     redirects but its content no longer indexes Dawn holdings.
    #   - pds-geosciences.wustl.edu does NOT host Dawn FC/VIR data
    #     (confirmed 404 on /dawn/) — that node hosts other missions
    #     (e.g. MESSENGER, GRAIL).
    #   - pds-imaging.jpl.nasa.gov does NOT host Dawn data either.
    #   - Live archive root: https://sbnarchive.psi.edu/pds3/dawn/fc/
    dawn_fc_vesta:
      description: >
        Dawn Framing Camera 2 (FC2) images of Vesta, PDS3 archive volumes.
      node: "PDS Small Bodies Node (sbnarchive.psi.edu)"
      instrument: FC2
      target: VESTA
      level: calibrated   # which sub-source fetch_framing_camera_images() uses by default
      raw:
        dataset_id: DAWN-A-FC2-2-EDR-VESTA-IMAGES-V1.0
        volume_id: DWNVFC2_1A
        base_url: https://sbnarchive.psi.edu/pds3/dawn/fc/DWNVFC2_1A/
        index_url: https://sbnarchive.psi.edu/pds3/dawn/fc/DWNVFC2_1A/INDEX/INDEX.TAB
        index_label_url: https://sbnarchive.psi.edu/pds3/dawn/fc/DWNVFC2_1A/INDEX/INDEX.LBL
      calibrated:
        dataset_id: DAWN-A-FC2-3-RDR-VESTA-IMAGES-V1.0
        volume_id: DWNVFC2_1B
        base_url: https://sbnarchive.psi.edu/pds3/dawn/fc/DWNVFC2_1B/
        index_url: https://sbnarchive.psi.edu/pds3/dawn/fc/DWNVFC2_1B/INDEX/INDEX.TAB
        index_label_url: https://sbnarchive.psi.edu/pds3/dawn/fc/DWNVFC2_1B/INDEX/INDEX.LBL

    # ------------------------------------------------------------------
    # Dawn VIR (Visible and InfraRed Mapping Spectrometer), Vesta phase.
    # Same node as FC: PDS Small Bodies Node (sbnarchive.psi.edu).
    # VIR has two independent channels (vis, ir), each archived as its own
    # volume, at two processing levels (raw EDR = *1A, calibrated RDR = *1B).
    # Default level is 'calibrated' (DWNVVIR_V1B / DWNVVIR_I1B) — raw EDR
    # is uncalibrated instrument DN, not reflectance-like data, and is not
    # valid input for the band-depth label scheme (see
    # ml/data/spectral_labeling.py and docs/month1_log.md, Slice 3's
    # data-quality-gate finding). 'raw' is kept configured as an
    # explicit alternative, not the default.
    dawn_vir_vesta:
      description: >
        Dawn VIR spectral cubes (VIS + IR channels) of Vesta, PDS3 archive
        volumes.
      node: "PDS Small Bodies Node (sbnarchive.psi.edu)"
      instrument: VIR
      target: VESTA
      level: calibrated  # which sub-source(s) fetch_vir_spectra() uses by default
      channels: [ir, vis]
      raw:
        ir:
          dataset_id: DAWN-A-VIR-2-EDR-IR-VESTA-SPECTRA-V1.0
          volume_id: DWNVVIR_I1A
          base_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_I1A/
          index_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_I1A/INDEX/INDEX.TAB
          index_label_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_I1A/INDEX/INDEX.LBL
        vis:
          dataset_id: DAWN-A-VIR-2-EDR-VIS-VESTA-SPECTRA-V1.0
          volume_id: DWNVVIR_V1A
          base_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_V1A/
          index_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_V1A/INDEX/INDEX.TAB
          index_label_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_V1A/INDEX/INDEX.LBL
      calibrated:
        ir:
          dataset_id: DAWN-A-VIR-3-RDR-IR-VESTA-SPECTRA-V1.0
          volume_id: DWNVVIR_I1B
          base_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_I1B/
          index_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_I1B/INDEX/INDEX.TAB
          index_label_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_I1B/INDEX/INDEX.LBL
        vis:
          dataset_id: DAWN-A-VIR-3-RDR-VIS-VESTA-SPECTRA-V1.0
          volume_id: DWNVVIR_V1B
          base_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_V1B/
          index_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_V1B/INDEX/INDEX.TAB
          index_label_url: https://sbnarchive.psi.edu/pds3/dawn/vir/DWNVVIR_V1B/INDEX/INDEX.LBL

  # Dawn Vesta mission-phase date windows, used by fetch_framing_camera_images()/
  # fetch_vir_spectra()'s `phase=` argument to filter INDEX.TAB rows by real
  # START_TIME instead of taking the chronologically-first N rows (which is
  # approach-phase OpNav data with no footprint geometry -- see
  # docs/month1_log.md, Slice 2). Start dates are directly verified against
  # the live archive's top-level DATA/ directory listings, which use two
  # different date-string conventions for the SAME real phase-start dates:
  #   FC:  DATA/FITS/<YYYYDDD>_<PHASE>/   (day-of-year)
  #   VIR: DATA/<YYYYMMDD>_<PHASE>/       (calendar date)
  # e.g. FC '2011272_HAMO' and VIR '20110929_HAMO' are the same real date
  # (day 272 of 2011 = 2011-09-29) -- confirmed by direct date-of-year
  # conversion, not assumed. End dates are each phase's next-phase start
  # date (i.e. windows are contiguous, [start, next start)).
  mission_phases:
    approach:            {start: "2011-05-03", stop: "2011-08-10"}
    survey:               {start: "2011-08-11", stop: "2011-08-30"}
    transfer_to_hamo:     {start: "2011-08-31", stop: "2011-09-28"}
    hamo:                 {start: "2011-09-29", stop: "2011-11-01"}
    # hamo_cycle1: the first HAMO imaging cycle only (both FC's
    # '2011272_CYCLE1' and VIR's '20110929_CYCLE1' start 2011-09-29;
    # VIR's '20111006_CYCLE2' starts 2011-10-06 -- confirmed live, see
    # docs/month1_log.md). Narrow, verified-overlapping window -- this is
    # the phase this project's re-acquisition pass actually used.
    hamo_cycle1:           {start: "2011-09-29", stop: "2011-10-05"}
    transfer_to_lamo:      {start: "2011-11-02", stop: "2011-12-11"}
    lamo:                  {start: "2011-12-12", stop: "2012-06-14"}
    # lamo_cycle4: the first LAMO imaging cycle with real, verified data
    # from BOTH instruments -- NOT "cycle1", and NOT the nominal
    # directory-name dates below (a real schedule slip, caught by the
    # phase filter returning 0 rows and diagnosed against the live
    # INDEX.TAB before re-guessing, not assumed):
    #   - FC's LAMO directory lists cycles 1-21 starting 2011347_CYCLE1
    #     (2011-12-13); VIR's LAMO directory (DWNVVIR_V1B/I1B) starts at
    #     20111231_CYCLE4 -- VIR cycles 1-3 are genuinely absent from the
    #     archive (the nominal 2011-12-31 CYCLE4 directory itself 404s).
    #   - Checking real START_TIME values directly in the volume-level
    #     INDEX.TAB (not directory names): VIR's "CYCLE4"-labeled data is
    #     ENTIRELY real-dated 2012-01-08 11:35-20:35 (a ~9 hour window,
    #     not the ~week its directory name implies) -- an 8-9 day slip
    #     from the nominal 2011-12-31 label.
    #   - FC's own 2011365_CYCLE4 (nominally the same date, 2011-12-31)
    #     is real-dated 2012-01-01 02:29 through just before 2012-01-08
    #     06:31, when FC's *own* CYCLE5 begins -- i.e. FC's real cycle4
    #     window ends right as VIR's real cycle4 window begins. The
    #     actual temporal overlap with VIR's one real day is FC's CYCLE5,
    #     not FC's CYCLE4, despite the matching nominal cycle number.
    # Bottom line: "same cycle number" does not mean "same real time" for
    # LAMO the way it happened to for HAMO. This window uses the real,
    # verified dates (with a 1-day buffer either side of VIR's single
    # real day), not the nominal directory-name dates.
    lamo_cycle4:           {start: "2012-01-07", stop: "2012-01-09"}
    # Slice 16: three additional, deliberately diversified real windows,
    # added specifically to break the acquisition-session and
    # fit-confidence confounds Slices 6-15 found in the original
    # hamo_cycle1 + lamo_cycle4 sample. Verified directly against the
    # real volume-level INDEX.TAB (parse_index_table(), unmodified) before
    # adding here, same discipline as every prior acquisition slice --
    # not assumed from directory names alone.
    #
    # hamo_cycle3: FC's '2011284_CYCLE3' and VIR's '20111011_CYCLE3' both
    # start 2011-10-11 (day 284); the next cycle (both instruments'
    # CYCLE4) starts 2011-10-16 -- no schedule slip here, unlike LAMO.
    # Real per-day session hours in this window (checked directly): 7-11
    # and 19-23 UTC -- genuinely different from hamo_cycle1's 1 and 13 UTC
    # sessions, not just a different date.
    hamo_cycle3:           {start: "2011-10-11", stop: "2011-10-16"}
    # hamo_cycle6: FC's '2011299_CYCLE6' and VIR's '20111026_CYCLE6' both
    # start 2011-10-26 (day 299); this is HAMO's LAST cycle, ending at
    # HAMO's own phase stop (2011-11-01) -- no CYCLE7 exists in either
    # instrument's real directory listing. Real per-day session hours:
    # 3-5 and 15-18 UTC -- a third, distinct time-of-day pattern from both
    # hamo_cycle1 (1, 13) and hamo_cycle3 (7-11, 19-23).
    hamo_cycle6:           {start: "2011-10-26", stop: "2011-11-01"}
    # lamo_window2: a real, denser LAMO window ~3 months after
    # lamo_cycle4, chosen because lamo_cycle4's real n=8 paired
    # observations were too thin to separate its own confidence-confound
    # (Slice 15) from a real effect. Verified directly against the real
    # volume-level INDEX.TAB: VIR IR has 120 real rows in this window
    # (2012-04-15T00:00 to 14:11), a single real day with multiple
    # sessions (hours 0, 5, 9, 13, 14 UTC) -- about 15x lamo_cycle4's
    # total row count. FC has 880 real INDEX.TAB rows in the same window
    # (2012-04-14T23:35 to 2012-04-17T14:45) -- includes the same
    # label/non-label row duplication Slice 11 found and fixed in
    # pds_acquisition.py, so the existing .LBL-only filter applies here
    # unchanged, not re-diagnosed.
    lamo_window2:          {start: "2012-04-13", stop: "2012-04-18"}
    hamo_2:                {start: "2012-06-15", stop: "2012-07-24"}
    transfer_to_ceres:     {start: "2012-07-25", stop: "2012-09-05"}

acquisition:
  user_agent: "AstroMineAI-DataAcquisition/0.1 (contact: akshatjha1981@gmail.com)"
  request_timeout_seconds: 60
  retries: 3
  retry_backoff_seconds: 2
