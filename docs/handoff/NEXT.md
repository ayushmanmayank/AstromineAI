<!--
This file is empty by design. It holds the NEXT task for the Claude
session working this branch to pick up — written by the other Claude
instance (the one with read-only clone access, reviewing via
docs/handoff/STATUS.md), relayed here by the human since that instance
can't push directly.

For the working session reading this file:
- Empty or content that looks stale/already-completed (cross-check
  against docs/handoff/STATUS.md's history first) = nothing to act on.
- Real content here = treat it as the next prompt: read it, execute it,
  then append a new entry to docs/handoff/STATUS.md when done.
-->
